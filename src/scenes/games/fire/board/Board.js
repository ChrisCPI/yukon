/* START OF COMPILED CODE */

import BaseContainer from "../../../base/BaseContainer";
/* START-USER-IMPORTS */
/* END-USER-IMPORTS */

export default class Board extends BaseContainer {

    constructor(scene, x, y) {
        super(scene, x ?? 760, y ?? 480);

        // board_space00001
        const board_space00001 = scene.add.image(-17, 308, "fire", "board/space00001");
        this.add(board_space00001);

        // board_space10001
        const board_space10001 = scene.add.image(202, 278, "fire", "board/space10001");
        this.add(board_space10001);

        // board_space20001
        const board_space20001 = scene.add.image(356, 225, "fire", "board/space20001");
        this.add(board_space20001);

        // board_space30001
        const board_space30001 = scene.add.image(433, 136, "fire", "board/space30001");
        this.add(board_space30001);

        // board_space40001
        const board_space40001 = scene.add.image(461, 5, "fire", "board/space40001");
        this.add(board_space40001);

        // board_space50001
        const board_space50001 = scene.add.image(434, -123, "fire", "board/space50001");
        this.add(board_space50001);

        // board_space60001
        const board_space60001 = scene.add.image(314, -186, "fire", "board/space60001");
        this.add(board_space60001);

        // board_space70001
        const board_space70001 = scene.add.image(192, -244, "fire", "board/space70001");
        this.add(board_space70001);

        // board_space80001
        const board_space80001 = scene.add.image(1, -264, "fire", "board/space80001");
        this.add(board_space80001);

        // board_space90001
        const board_space90001 = scene.add.image(-192, -249, "fire", "board/space90001");
        this.add(board_space90001);

        // board_space100001
        const board_space100001 = scene.add.image(-323, -189, "fire", "board/space100001");
        this.add(board_space100001);

        // board_space110001
        const board_space110001 = scene.add.image(-443, -126, "fire", "board/space110001");
        this.add(board_space110001);

        // board_space120001
        const board_space120001 = scene.add.image(-469, -1, "fire", "board/space120001");
        this.add(board_space120001);

        // board_space130001
        const board_space130001 = scene.add.image(-441, 130, "fire", "board/space130001");
        this.add(board_space130001);

        // board_space140001
        const board_space140001 = scene.add.image(-368, 220, "fire", "board/space140001");
        this.add(board_space140001);

        // board_space150001
        const board_space150001 = scene.add.image(-240, 293, "fire", "board/space150001");
        this.add(board_space150001);

        /* START-USER-CTR-CODE */
        /* END-USER-CTR-CODE */
    }

    /* START-USER-CODE */
    /* END-USER-CODE */
}

/* END OF COMPILED CODE */